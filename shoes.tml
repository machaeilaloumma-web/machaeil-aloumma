<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>StepWise – Premium Modest Footwear for the Whole Family</title>
  <meta name="description" content="Family-friendly shoe store offering stylish, modest footwear for men, women, and children. Free shipping, secure checkout, and size guides included." />
  <!-- 
       💡 How to test:
       1. Save this file as `index.html`
       2. Open it directly in any modern browser (Chrome, Firefox, Edge, Safari)
       3. Works offline — all JS/CSS embedded, images as lightweight data URLs
  -->
  <style>
    /* ===== RESET & BASE STYLES ===== */
    :root {
      --primary: #4a6fa5;
      --primary-dark: #3a5a80;
      --secondary: #d9a76e;
      --light: #f8f9fa;
      --dark: #212529;
      --gray: #6c757d;
      --light-gray: #e9ecef;
      --success: #28a745;
      --danger: #dc3545;
      --border-radius: 8px;
      --shadow: 0 4px 12px rgba(0,0,0,0.08);
      --transition: all 0.3s ease;
      --font-sans: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: var(--font-sans);
      line-height: 1.6;
      color: var(--dark);
      background-color: #fafafa;
      overflow-x: hidden;
    }

    img {
      max-width: 100%;
      height: auto;
      display: block;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    button, .btn {
      cursor: pointer;
      border: none;
      font-family: inherit;
      font-weight: 600;
      padding: 12px 24px;
      border-radius: var(--border-radius);
      transition: var(--transition);
      outline-offset: 2px;
    }

    .btn-primary {
      background-color: var(--primary);
      color: white;
    }
    .btn-primary:hover, .btn-primary:focus {
      background-color: var(--primary-dark);
      box-shadow: 0 4px 8px rgba(74, 111, 165, 0.3);
    }

    .btn-outline {
      background: transparent;
      border: 2px solid var(--primary);
      color: var(--primary);
    }
    .btn-outline:hover, .btn-outline:focus {
      background-color: rgba(74, 111, 165, 0.05);
    }

    .container {
      width: 100%;
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1.5rem;
    }

    .sr-only {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border: 0;
    }

    /* ===== HEADER & NAV ===== */
    header {
      background-color: white;
      box-shadow: 0 2px 6px rgba(0,0,0,0.05);
      position: sticky;
      top: 0;
      z-index: 1000;
    }

    .top-bar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 0;
    }

    .logo {
      font-size: 1.75rem;
      font-weight: 800;
      color: var(--primary);
    }
    .logo span {
      color: var(--secondary);
    }

    .header-right {
      display: flex;
      gap: 1.5rem;
      align-items: center;
    }

    .search-bar {
      display: flex;
      align-items: center;
    }

    .search-input {
      padding: 8px 12px;
      border: 1px solid #ddd;
      border-radius: 20px;
      font-size: 0.9rem;
      width: 200px;
      transition: width 0.3s;
    }
    .search-input:focus {
      outline: none;
      border-color: var(--primary);
      width: 240px;
    }

    .cart-btn {
      position: relative;
      background: none;
      border: none;
      font-size: 1.5rem;
      color: var(--gray);
      cursor: pointer;
      padding: 8px;
    }
    .cart-count {
      position: absolute;
      top: -6px;
      right: -6px;
      background-color: var(--danger);
      color: white;
      font-size: 0.75rem;
      width: 18px;
      height: 18px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    /* ===== HERO & SLIDER ===== */
    .hero {
      background: linear-gradient(rgba(74, 111, 165, 0.85), rgba(58, 90, 128, 0.9)), 
                  url("data:image/svg+xml,%3Csvg width='600' height='400' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='600' height='400' fill='%23f0f4f8'/%3E%3Ccircle cx='300' cy='200' r='80' fill='%23d9a76e' opacity='0.2'/%3E%3C/svg%3E") center/cover;
      color: white;
      padding: 5rem 1.5rem;
      text-align: center;
    }

    .hero h1 {
      font-size: 2.8rem;
      margin-bottom: 1rem;
      font-weight: 800;
    }
    .hero p {
      font-size: 1.25rem;
      max-width: 700px;
      margin: 0 auto 2rem;
      opacity: 0.9;
    }

    .slider-section {
      position: relative;
      max-width: 900px;
      margin: 2rem auto;
      padding: 0 1rem;
    }

    .slider {
      position: relative;
      overflow: hidden;
      border-radius: var(--border-radius);
      box-shadow: var(--shadow);
    }

    .slides {
      display: flex;
      transition: transform 0.5s ease-in-out;
    }

    .slide {
      min-width: 100%;
      flex-shrink: 0;
    }

    .slide img {
      width: 100%;
      aspect-ratio: 16 / 9;
      object-fit: cover;
    }

    .slider-controls {
      display: flex;
      justify-content: center;
      gap: 0.5rem;
      margin-top: 1rem;
    }

    .slider-dot {
      width: 12px;
      height: 12px;
      border-radius: 50%;
      background-color: rgba(255,255,255,0.5);
      border: none;
      cursor: pointer;
    }
    .slider-dot.active {
      background-color: white;
    }

    .slider-arrow {
      position: absolute;
      top: 50%;
      transform: translateY(-50%);
      background: rgba(255,255,255,0.8);
      color: var(--dark);
      width: 40px;
      height: 40px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.25rem;
      font-weight: bold;
      cursor: pointer;
      border: none;
    }
    .slider-arrow:hover {
      background: white;
    }
    .prev { left: 1rem; }
    .next { right: 1rem; }

    /* ===== FILTERS ===== */
    .filters {
      background: white;
      padding: 1.5rem;
      border-radius: var(--border-radius);
      margin: 2rem 0;
      box-shadow: var(--shadow);
    }

    .filter-group {
      margin-bottom: 1rem;
    }

    .filter-label {
      display: block;
      margin-bottom: 0.5rem;
      font-weight: 600;
    }

    .filter-row {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
      align-items: center;
    }

    .category-filters button {
      padding: 8px 16px;
      background: var(--light-gray);
      border-radius: 20px;
      font-size: 0.9rem;
    }
    .category-filters button.active {
      background: var(--primary);
      color: white;
    }

    .price-slider {
      flex: 1;
      min-width: 200px;
    }

    .price-display {
      font-weight: 600;
      margin-left: 1rem;
    }

    .sale-toggle {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    .toggle-switch {
      position: relative;
      display: inline-block;
      width: 48px;
      height: 24px;
    }
    .toggle-switch input {
      opacity: 0;
      width: 0;
      height: 0;
    }
    .slider-toggle {
      position: absolute;
      cursor: pointer;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background-color: #ccc;
      transition: .3s;
      border-radius: 24px;
    }
    .slider-toggle:before {
      position: absolute;
      content: "";
      height: 16px;
      width: 16px;
      left: 4px;
      bottom: 4px;
      background-color: white;
      transition: .3s;
      border-radius: 50%;
    }
    input:checked + .slider-toggle {
      background-color: var(--primary);
    }
    input:checked + .slider-toggle:before {
      transform: translateX(24px);
    }

    /* ===== PRODUCT GRID ===== */
    .products-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
      gap: 2rem;
      margin: 2rem 0;
    }

    .product-card {
      background: white;
      border-radius: var(--border-radius);
      overflow: hidden;
      box-shadow: var(--shadow);
      transition: var(--transition);
    }
    .product-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 20px rgba(0,0,0,0.12);
    }

    .product-image {
      position: relative;
      height: 200px;
      overflow: hidden;
    }
    .product-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .discount-badge {
      position: absolute;
      top: 12px;
      left: 12px;
      background: var(--danger);
      color: white;
      font-size: 0.75rem;
      font-weight: bold;
      padding: 4px 8px;
      border-radius: 4px;
    }

    .product-info {
      padding: 1.25rem;
    }

    .product-title {
      font-size: 1.1rem;
      margin-bottom: 0.5rem;
    }

    .price-section {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      margin: 0.75rem 0;
    }

    .original-price {
      text-decoration: line-through;
      color: var(--gray);
      font-size: 0.9rem;
    }

    .discounted-price {
      font-weight: 700;
      font-size: 1.25rem;
      color: var(--primary-dark);
    }

    .rating {
      color: #ffc107;
      margin-bottom: 0.75rem;
    }

    .add-to-cart {
      width: 100%;
      background: var(--light-gray);
      color: var(--dark);
      font-weight: 600;
    }
    .add-to-cart:hover {
      background: var(--primary);
      color: white;
    }

    /* ===== SERVICES SECTION ===== */
    .services {
      background: var(--light);
      padding: 3rem 0;
      margin: 3rem 0;
    }

    .services-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 2rem;
    }

    .service-card {
      text-align: center;
      padding: 1.5rem;
    }
    .service-icon {
      font-size: 2.5rem;
      color: var(--primary);
      margin-bottom: 1rem;
    }

    /* ===== FOOTER ===== */
    footer {
      background: var(--dark);
      color: white;
      padding: 3rem 0 2rem;
    }

    .footer-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 2rem;
      margin-bottom: 2rem;
    }

    .footer-section h3 {
      font-size: 1.25rem;
      margin-bottom: 1.25rem;
      position: relative;
      padding-bottom: 0.5rem;
    }
    .footer-section h3::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 40px;
      height: 3px;
      background: var(--secondary);
    }

    .footer-links {
      list-style: none;
    }
    .footer-links li {
      margin-bottom: 0.75rem;
    }
    .footer-links a:hover {
      color: var(--secondary);
    }

    .newsletter {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }
    .newsletter input {
      padding: 12px;
      border-radius: var(--border-radius);
      border: none;
    }
    .newsletter button {
      background: var(--primary);
    }

    .footer-bottom {
      text-align: center;
      padding-top: 2rem;
      border-top: 1px solid rgba(255,255,255,0.1);
      font-size: 0.9rem;
      color: #adb5bd;
    }

    /* ===== CART MODAL ===== */
    .cart-modal {
      position: fixed;
      top: 0;
      right: -100%;
      width: 380px;
      height: 100vh;
      background: white;
      box-shadow: -4px 0 20px rgba(0,0,0,0.15);
      z-index: 2000;
      transition: right 0.4s ease;
      overflow-y: auto;
    }
    .cart-modal.open {
      right: 0;
    }

    .cart-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1.5rem;
      border-bottom: 1px solid #eee;
    }

    .cart-close {
      background: none;
      border: none;
      font-size: 1.5rem;
      cursor: pointer;
      color: var(--gray);
    }

    .cart-items {
      padding: 0 1.5rem;
    }

    .cart-item {
      display: flex;
      gap: 1rem;
      padding: 1.25rem 0;
      border-bottom: 1px solid #eee;
    }

    .cart-item-image {
      width: 70px;
      height: 70px;
      flex-shrink: 0;
      border-radius: var(--border-radius);
      overflow: hidden;
    }

    .cart-item-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .cart-item-info {
      flex: 1;
    }

    .cart-item-title {
      font-weight: 600;
      margin-bottom: 0.25rem;
    }

    .cart-item-price {
      font-weight: 700;
      color: var(--primary-dark);
      margin-bottom: 0.5rem;
    }

    .quantity-controls {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .qty-btn {
      width: 30px;
      height: 30px;
      background: var(--light-gray);
      border-radius: 4px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
    }

    .qty-input {
      width: 40px;
      text-align: center;
      border: 1px solid #ddd;
      border-radius: 4px;
      padding: 4px;
    }

    .remove-item {
      color: var(--danger);
      background: none;
      border: none;
      font-size: 0.875rem;
      margin-top: 0.5rem;
    }

    .cart-footer {
      padding: 1.5rem;
      border-top: 1px solid #eee;
    }

    .subtotal {
      font-size: 1.25rem;
      font-weight: 700;
      margin-bottom: 1rem;
    }

    /* ===== RESPONSIVE ===== */
    @media (max-width: 768px) {
      .hero h1 { font-size: 2.2rem; }
      .hero p { font-size: 1.1rem; }
      .top-bar { flex-direction: column; gap: 1rem; }
      .search-input { width: 100%; max-width: 300px; }
      .cart-modal { width: 100%; }
      .slider-arrow { display: none; }
    }

    @media (max-width: 480px) {
      .hero { padding: 3rem 1rem; }
      .hero h1 { font-size: 1.8rem; }
      .filter-row { flex-direction: column; }
      .price-slider, .price-display { width: 100%; }
    }
  </style>
</head>
<body>
  <!-- ===== HEADER ===== -->
  <header>
    <div class="container">
      <div class="top-bar">
        <a href="#" class="logo">Step<span>Wise</span></a>
        <div class="header-right">
          <div class="search-bar">
            <input 
              type="text" 
              id="searchInput" 
              class="search-input" 
              placeholder="Search shoes..." 
              aria-label="Search products"
            />
          </div>
          <button id="cartBtn" class="cart-btn" aria-label="Shopping cart">
            <span>🛒</span>
            <span id="cartCount" class="cart-count">0</span>
          </button>
        </div>
      </div>
    </div>
  </header>

  <!-- ===== HERO & SLIDER ===== -->
  <section class="hero">
    <div class="container">
      <h1>Step Into Comfort & Modesty</h1>
      <p>Premium footwear for men, women, and children — designed with quality, style, and family values in mind.</p>
      <a href="#products" class="btn btn-primary">Shop New Arrivals</a>
    </div>
  </section>

  <section class="slider-section">
    <div class="slider">
      <div class="slides" id="slidesContainer">
        <!-- Slides populated by JS -->
      </div>
      <button class="slider-arrow prev" aria-label="Previous slide">‹</button>
      <button class="slider-arrow next" aria-label="Next slide">›</button>
      <div class="slider-controls" id="dotsContainer">
        <!-- Dots populated by JS -->
      </div>
    </div>
  </section>

  <!-- ===== FILTERS ===== -->
  <div class="container">
    <section class="filters">
      <div class="filter-row">
        <div class="filter-group">
          <label class="filter-label">Category</label>
          <div class="category-filters" id="categoryFilters">
            <button data-category="all" class="active">All</button>
            <button data-category="men">Men</button>
            <button data-category="women">Women</button>
            <button data-category="kids">Kids</button>
          </div>
        </div>

        <div class="filter-group">
          <label class="filter-label">Price: <span id="priceRangeDisplay">$0 – $150</span></label>
          <div class="price-slider">
            <input 
              type="range" 
              id="priceSlider" 
              min="0" 
              max="150" 
              value="150"
              step="10"
              aria-label="Filter by maximum price"
            />
          </div>
        </div>

        <div class="filter-group sale-toggle">
          <span>Show Sale Items Only</span>
          <label class="toggle-switch">
            <input type="checkbox" id="saleToggle">
            <span class="slider-toggle"></span>
          </label>
        </div>
      </div>
    </section>

    <!-- ===== PRODUCT GRID ===== -->
    <section id="products">
      <h2 class="sr-only">Featured Products</h2>
      <div class="products-grid" id="productsGrid">
        <!-- Products populated by JS -->
      </div>
    </section>
  </div>

  <!-- ===== SERVICES ===== -->
  <section class="services">
    <div class="container">
      <h2 class="sr-only">Why Shop With Us</h2>
      <div class="services-grid">
        <div class="service-card">
          <div class="service-icon">🚚</div>
          <h3>Free Shipping</h3>
          <p>On all orders over $50. Delivered to your door in 3–5 business days.</p>
        </div>
        <div class="service-card">
          <div class="service-icon">🔒</div>
          <h3>Secure Payments</h3>
          <p>Encrypted checkout with credit card, PayPal, and Apple Pay.</p>
        </div>
        <div class="service-card">
          <div class="service-icon">📏</div>
          <h3>Size Guide</h3>
          <p>Accurate fit guaranteed — use our printable guide or virtual assistant.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== FOOTER ===== -->
  <footer>
    <div class="container">
      <div class="footer-grid">
        <div class="footer-section">
          <h3>StepWise</h3>
          <p>Family-owned since 2010. Dedicated to modest, high-quality footwear for every step of life.</p>
        </div>
        <div class="footer-section">
          <h3>Quick Links</h3>
          <ul class="footer-links">
            <li><a href="#">Home</a></li>
            <li><a href="#">Men</a></li>
            <li><a href="#">Women</a></li>
            <li><a href="#">Kids</a></li>
            <li><a href="#">Sale</a></li>
          </ul>
        </div>
        <div class="footer-section">
          <h3>Support</h3>
          <ul class="footer-links">
            <li><a href="#">Contact Us</a></li>
            <li><a href="#">FAQs</a></li>
            <li><a href="#">Shipping Policy</a></li>
            <li><a href="#">Returns & Exchanges</a></li>
            <li><a href="#">Size Guide</a></li>
          </ul>
        </div>
        <div class="footer-section">
          <h3>Newsletter</h3>
          <form id="newsletterForm" class="newsletter">
            <input 
              type="email" 
              id="newsletterEmail" 
              placeholder="Your email" 
              required 
              aria-label="Enter your email for updates"
            />
            <button type="submit" class="btn btn-primary">Subscribe</button>
          </form>
        </div>
      </div>
      <div class="footer-bottom">
        <p>&copy; 2025 StepWise Shoes. All rights reserved. | Designed with care for families.</p>
      </div>
    </div>
  </footer>

  <!-- ===== CART MODAL ===== -->
  <div id="cartModal" class="cart-modal">
    <div class="cart-header">
      <h2>Your Cart</h2>
      <button id="closeCart" class="cart-close" aria-label="Close cart">×</button>
    </div>
    <div class="cart-items" id="cartItemsList">
      <!-- Cart items populated by JS -->
    </div>
    <div class="cart-footer">
      <div class="subtotal">Subtotal: <span id="cartSubtotal">$0.00</span></div>
      <button id="checkoutBtn" class="btn btn-primary" disabled>Proceed to Checkout</button>
    </div>
  </div>

  <!-- ===== SCRIPTS ===== -->
  <script>
    // ===== DATA: Slider Images (modest, family-friendly) =====
    const sliderImages = [
      { src: "data:image/svg+xml,%3Csvg width='800' height='450' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='800' height='450' fill='%23e6f0fa'/%3E%3Crect x='100' y='150' width='200' height='100' rx='10' fill='%234a6fa5'/%3E%3Ccircle cx='200' cy='200' r='40' fill='%23d9a76e'/%3E%3Ctext x='50%25' y='80' text-anchor='middle' fill='%23212529' font-size='24' font-family='sans-serif'%3EMen's Formal Shoes%3C/text%3E%3C/svg%3E", alt: "Elegant men's leather formal shoes" },
      { src: "data:image/svg+xml,%3Csvg width='800' height='450' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='800' height='450' fill='%23f9f3eb'/%3E%3Cellipse cx='400' cy='225' rx='120' ry='80' fill='%23d9a76e' opacity='0.3'/%3E%3Crect x='300' y='200' width='200' height='60' rx='8' fill='%234a6fa5'/%3E%3Ctext x='50%25' y='100' text-anchor='middle' fill='%23212529' font-size='24' font-family='sans-serif'%3EWomen's Comfort Flats%3C/text%3E%3C/svg%3E", alt: "Stylish women's flat shoes in neutral tones" },
      { src: "data:image/svg+xml,%3Csvg width='800' height='450' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='800' height='450' fill='%23f0f7ff'/%3E%3Ccircle cx='200' cy='250' r='60' fill='%23a8d8ea'/%3E%3Ccircle cx='600' cy='200' r='50' fill='%23ffb3ba'/%3E%3Crect x='170' y='320' width='60' height='30' fill='%234a6fa5'/%3E%3Crect x='570' y='270' width='60' height='30' fill='%23d9a76e'/%3E%3Ctext x='50%25' y='100' text-anchor='middle' fill='%23212529' font-size='24' font-family='sans-serif'%3EKids' Sneakers%3C/text%3E%3C/svg%3E", alt: "Colorful kids' sneakers for boys and girls" },
      { src: "data:image/svg+xml,%3Csvg width='800' height='450' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='800' height='450' fill='%23fff9f0'/%3E%3Cpath d='M150 300 Q400 100 650 300 T150 300' fill='%23d9a76e' opacity='0.2'/%3E%3Crect x='350' y='250' width='100' height='80' rx='8' fill='%234a6fa5'/%3E%3Ctext x='50%25' y='120' text-anchor='middle' fill='%23212529' font-size='24' font-family='sans-serif'%3ESandals Collection%3C/text%3E%3C/svg%3E", alt: "Summer sandals for the whole family" },
      { src: "data:image/svg+xml,%3Csvg width='800' height='450' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='800' height='450' fill='%23eef6f1'/%3E%3Crect x='200' y='200' width='400' height='120' rx='12' fill='%23d9a76e'/%3E%3Crect x='300' y='230' width='200' height='60' fill='%234a6fa5'/%3E%3Ctext x='50%25' y='130' text-anchor='middle' fill='%23212529' font-size='24' font-family='sans-serif'%3EWork & School Shoes%3C/text%3E%3C/svg%3E", alt: "Durable work and school shoes" }
    ];

    // ===== DATA: Products (modest, family-appropriate) =====
    const products = [
      {
        id: 1,
        name: "Classic Oxford Shoes",
        category: "men",
        price: 120.00,
        discount: 15,
        rating: 4.5,
        image: "data:image/svg+xml,%3Csvg width='300' height='300' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='300' height='300' fill='%23f8f9fa'/%3E%3Crect x='80' y='120' width='140' height='60' rx='8' fill='%23343a40'/%3E%3Crect x='90' y='130' width='120' height='40' fill='%23495057'/%3E%3C/svg%3E",
        alt: "Men's black leather Oxford shoes"
      },
      {
        id: 2,
        name: "Loafers with Tassel",
        category: "men",
        price: 95.00,
        discount: 0,
        rating: 4.2,
        image: "data:image/svg+xml,%3Csvg width='300' height='300' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='300' height='300' fill='%23f8f9fa'/%3E%3Cellipse cx='150' cy='180' rx='70' ry='40' fill='%238c6d46'/%3E%3Ccircle cx='150' cy='150' r='15' fill='%235d4a2e'/%3E%3C/svg%3E",
        alt: "Brown leather men's loafers with tassel detail"
      },
      {
        id: 3,
        name: "Formal Derby Shoes",
        category: "men",
        price: 135.00,
        discount: 20,
        rating: 4.7,
        image: "data:image/svg+xml,%3Csvg width='300' height='300' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='300' height='300' fill='%23f8f9fa'/%3E%3Cpath d='M100 200 Q150 150 200 200 T100 200' fill='%23212529'/%3E%3Crect x='120' y='180' width='60' height='40' fill='%23343a40'/%3E%3C/svg%3E",
        alt: "Men's black Derbys with brogue detailing"
      },
      {
        id: 4,
        name: "Ballet Flats",
        category: "women",
        price: 65.00,
        discount: 25,
        rating: 4.8,
        image: "data:image/svg+xml,%3Csvg width='300' height='300' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='300' height='300' fill='%23f8f9fa'/%3E%3Cellipse cx='150' cy='200' rx='60' ry='30' fill='%23c9a98d'/%3E%3Ccircle cx='150' cy='160' r='20' fill='%23d9a76e'/%3E%3C/svg%3E",
        alt: "Nude ballet flats for women"
      },
      {
        id: 5,
        name: "Block Heel Pumps",
        category: "women",
        price: 89.00,
        discount: 10,
        rating: 4.4,
        image: "data:image/svg+xml,%3Csvg width='300' height='300' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='300' height='300' fill='%23f8f9fa'/%3E%3Cpath d='M120 220 L180 220 L160 180 L140 180 Z' fill='%236c757d'/%3E%3Crect x='135' y='220' width='30' height='40' fill='%23495057'/%3E%3C/svg%3E",
        alt: "Gray block heel pumps"
      },
      {
        id: 6,
        name: "Comfort Loafers",
        category: "women",
        price: 75.00,
        discount: 0,
        rating: 4.6,
        image: "data:image/svg+xml,%3Csvg width='300' height='300' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='300' height='300' fill='%23f8f9fa'/%3E%3Cellipse cx='150' cy='190' rx='65' ry='35' fill='%23a8d8ea'/%3E%3Ccircle cx='150' cy='155' r='18' fill='%237ab8d9'/%3E%3C/svg%3E",
        alt: "Blue women's comfort loafers"
      },
      {
        id: 7,
        name: "Velcro Sneakers",
        category: "kids",
        price: 45.00,
        discount: 30,
        rating: 4.9,
        image: "data:image/svg+xml,%3Csvg width='300' height='300' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='300' height='300' fill='%23f8f9fa'/%3E%3Crect x='90' y='160' width='120' height='60' rx='10' fill='%23ffb3ba'/%3E%3Crect x='110' y='170' width='80' height='40' fill='%23ff8fab'/%3E%3Crect x='140' y='200' width='20' height='10' rx='2' fill='%23343a40'/%3E%3C/svg%3E",
        alt: "Pink kids' sneakers with velcro straps"
      },
      {
        id: 8,
        name: "School Mary Janes",
        category: "kids",
        price: 55.00,
        discount: 0,
        rating: 4.5,
        image: "data:image/svg+xml,%3Csvg width='300' height='300' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='300' height='300' fill='%23f8f9fa'/%3E%3Cpath d='M100 200 Q150 160 200 200 T100 200' fill='%234a6fa5'/%3E%3Crect x='125' y='190' width='50' height='25' fill='%23212529'/%3E%3C/svg%3E",
        alt: "Navy blue Mary Jane school shoes"
      },
      {
        id: 9,
        name: "First Walkers",
        category: "kids",
        price: 40.00,
        discount: 15,
        rating: 4.7,
        image: "data:image/svg+xml,%3Csvg width='300' height='300' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='300' height='300' fill='%23f8f9fa'/%3E%3Cellipse cx='150' cy='190' rx='50' ry='25' fill='%23a8d8ea'/%3E%3Ccircle cx='130' cy='170' r='10' fill='%23212529'/%3E%3Ccircle cx='170' cy='170' r='10' fill='%23212529'/%3E%3C/svg%3E",
        alt: "Soft-soled blue first walker shoes for toddlers"
      }
    ];

    // ===== DOM ELEMENTS =====
    const slidesContainer = document.getElementById('slidesContainer');
    const dotsContainer = document.getElementById('dotsContainer');
    const productsGrid = document.getElementById('productsGrid');
    const searchInput = document.getElementById('searchInput');
    const categoryFilters = document.querySelectorAll('#categoryFilters button');
    const priceSlider = document.getElementById('priceSlider');
    const priceRangeDisplay = document.getElementById('priceRangeDisplay');
    const saleToggle = document.getElementById('saleToggle');
    const cartBtn = document.getElementById('cartBtn');
    const cartModal = document.getElementById('cartModal');
    const closeCart = document.getElementById('closeCart');
    const cartItemsList = document.getElementById('cartItemsList');
    const cartSubtotal = document.getElementById('cartSubtotal');
    const cartCountEl = document.getElementById('cartCount');
    const checkoutBtn = document.getElementById('checkoutBtn');
    const newsletterForm = document.getElementById('newsletterForm');
    const newsletterEmail = document.getElementById('newsletterEmail');
    const prevBtn = document.querySelector('.prev');
    const nextBtn = document.querySelector('.next');

    // ===== INIT: Slider =====
    function initSlider() {
      // Create slides
      sliderImages.forEach((img, index) => {
        const slide = document.createElement('div');
        slide.className = 'slide';
        slide.innerHTML = `<img src="${img.src}" alt="${img.alt}" />`;
        slidesContainer.appendChild(slide);
      });

      // Create dots
      sliderImages.forEach((_, index) => {
        const dot = document.createElement('button');
        dot.className = 'slider-dot';
        dot.setAttribute('aria-label', `Go to slide ${index + 1}`);
        dot.addEventListener('click', () => goToSlide(index));
        dotsContainer.appendChild(dot);
      });

      updateDots(0);
    }

    let currentSlide = 0;
    const totalSlides = sliderImages.length;

    function goToSlide(index) {
      currentSlide = ((index % totalSlides) + totalSlides) % totalSlides;
      slidesContainer.style.transform = `translateX(-${currentSlide * 100}%)`;
      updateDots(currentSlide);
    }

    function nextSlide() { goToSlide(currentSlide + 1); }
    function prevSlide() { goToSlide(currentSlide - 1); }

    function updateDots(index) {
      document.querySelectorAll('.slider-dot').forEach((dot, i) => {
        dot.classList.toggle('active', i === index);
      });
    }

    // Auto-advance
    let slideInterval = setInterval(nextSlide, 5000);

    // Pause on hover
    const slider = document.querySelector('.slider');
    slider.addEventListener('mouseenter', () => clearInterval(slideInterval));
    slider.addEventListener('mouseleave', () => {
      slideInterval = setInterval(nextSlide, 5000);
    });

    // Keyboard navigation
    document.addEventListener('keydown', (e) => {
      if (e.key === 'ArrowRight') nextSlide();
      if (e.key === 'ArrowLeft') prevSlide();
    });

    // ===== INIT: Products Grid =====
    function renderProducts(filteredProducts = products) {
      productsGrid.innerHTML = '';
      filteredProducts.forEach(product => {
        const discountAmount = product.discount ? (product.price * product.discount / 100) : 0;
        const finalPrice = product.price - discountAmount;
        const ratingStars = '★'.repeat(Math.floor(product.rating)) + 
                           '☆'.repeat(5 - Math.floor(product.rating));

        const productCard = document.createElement('article');
        productCard.className = 'product-card';
        productCard.innerHTML = `
          <div class="product-image">
            <img src="${product.image}" alt="${product.alt}" loading="lazy">
            ${product.discount ? `<span class="discount-badge">${product.discount}% OFF</span>` : ''}
          </div>
          <div class="product-info">
            <h3 class="product-title">${product.name}</h3>
            <div class="rating" aria-label="${product.rating} out of 5 stars">${ratingStars}</div>
            <div class="price-section">
              ${product.discount ? `<span class="original-price">$${product.price.toFixed(2)}</span>` : ''}
              <span class="discounted-price">$${finalPrice.toFixed(2)}</span>
            </div>
            <button class="btn add-to-cart" data-id="${product.id}">Add to Cart</button>
          </div>
        `;
        productsGrid.appendChild(productCard);
      });

      // Add event listeners to new buttons
      document.querySelectorAll('.add-to-cart').forEach(btn => {
        btn.addEventListener('click', () => addToCart(Number(btn.dataset.id)));
      });
    }

    // ===== CART MANAGEMENT =====
    let cart = JSON.parse(localStorage.getItem('cart')) || [];

    function updateCartUI() {
      // Update count badge
      const totalItems = cart.reduce((sum, item) => sum + item.quantity, 0);
      cartCountEl.textContent = totalItems;
      cartCountEl.style.display = totalItems > 0 ? 'block' : 'none';

      // Enable checkout if items exist
      checkoutBtn.disabled = totalItems === 0;

      // Update subtotal
      const subtotal = cart.reduce((sum, item) => {
        const product = products.find(p => p.id === item.id);
        const discount = product.discount ? (product.price * product.discount / 100) : 0;
        const finalPrice = product.price - discount;
        return sum + (finalPrice * item.quantity);
      }, 0);
      cartSubtotal.textContent = `$${subtotal.toFixed(2)}`;

      // Render cart items
      renderCartItems();
    }

    function renderCartItems() {
      if (cart.length === 0) {
        cartItemsList.innerHTML = `<p style="padding:1.5rem;text-align:center;color:#6c757d;">Your cart is empty.</p>`;
        return;
      }

      cartItemsList.innerHTML = '';
      cart.forEach(item => {
        const product = products.find(p => p.id === item.id);
        const discount = product.discount ? (product.price * product.discount / 100) : 0;
        const finalPrice = product.price - discount;
        const itemTotal = finalPrice * item.quantity;

        const cartItemEl = document.createElement('div');
        cartItemEl.className = 'cart-item';
        cartItemEl.innerHTML = `
          <div class="cart-item-image">
            <img src="${product.image}" alt="${product.alt}">
          </div>
          <div class="cart-item-info">
            <div class="cart-item-title">${product.name}</div>
            <div class="cart-item-price">$${finalPrice.toFixed(2)} × <span id="qty-${item.id}">${item.quantity}</span></div>
            <div class="quantity-controls">
              <button class="qty-btn dec" data-id="${item.id}">−</button>
              <input type="number" class="qty-input" value="${item.quantity}" min="1" data-id="${item.id}">
              <button class="qty-btn inc" data-id="${item.id}">+</button>
            </div>
            <button class="remove-item" data-id="${item.id}">Remove</button>
          </div>
        `;
        cartItemsList.appendChild(cartItemEl);
      });

      // Add event listeners
      document.querySelectorAll('.dec').forEach(btn => {
        btn.addEventListener('click', () => updateQuantity(Number(btn.dataset.id), -1));
      });
      document.querySelectorAll('.inc').forEach(btn => {
        btn.addEventListener('click', () => updateQuantity(Number(btn.dataset.id), 1));
      });
      document.querySelectorAll('.qty-input').forEach(input => {
        input.addEventListener('change', (e) => {
          const id = Number(e.target.dataset.id);
          const qty = Math.max(1, parseInt(e.target.value) || 1);
          updateQuantity(id, qty - getCartItemQuantity(id));
        });
      });
      document.querySelectorAll('.remove-item').forEach(btn => {
        btn.addEventListener('click', () => removeFromCart(Number(btn.dataset.id)));
      });
    }

    function getCartItemQuantity(id) {
      const item = cart.find(i => i.id === id);
      return item ? item.quantity : 0;
    }

    function addToCart(id) {
      const existing = cart.find(item => item.id === id);
      if (existing) {
        existing.quantity += 1;
      } else {
        cart.push({ id, quantity: 1 });
      }
      saveCart();
      updateCartUI();
      // Visual feedback
      const btn = document.querySelector(`.add-to-cart[data-id="${id}"]`);
      const originalText = btn.textContent;
      btn.textContent = 'Added!';
      btn.style.backgroundColor = '#28a745';
      setTimeout(() => {
        btn.textContent = originalText;
        btn.style.backgroundColor = '';
      }, 1500);
    }

    function updateQuantity(id, delta) {
      const item = cart.find(i => i.id === id);
      if (!item) return;
      item.quantity += delta;
      if (item.quantity < 1) item.quantity = 1;
      saveCart();
      updateCartUI();
    }

    function removeFromCart(id) {
      cart = cart.filter(item => item.id !== id);
      saveCart();
      updateCartUI();
    }

    function saveCart() {
      localStorage.setItem('cart', JSON.stringify(cart));
    }

    // ===== FILTER & SEARCH =====
    function applyFilters() {
      const searchTerm = searchInput.value.toLowerCase().trim();
      const selectedCategory = document.querySelector('#categoryFilters .active').dataset.category;
      const maxPrice = parseFloat(priceSlider.value);
      const showSaleOnly = saleToggle.checked;

      const filtered = products.filter(product => {
        // Search
        if (searchTerm && !product.name.toLowerCase().includes(searchTerm)) return false;

        // Category
        if (selectedCategory !== 'all' && product.category !== selectedCategory) return false;

        // Price
        const finalPrice = product.price * (1 - (product.discount || 0) / 100);
        if (finalPrice > maxPrice) return false;

        // Sale
        if (showSaleOnly && !product.discount) return false;

        return true;
      });

      renderProducts(filtered);
    }

    // ===== EVENT LISTENERS =====
    // Slider controls
    prevBtn.addEventListener('click', prevSlide);
    nextBtn.addEventListener('click', nextSlide);

    // Category filters
    categoryFilters.forEach(btn => {
      btn.addEventListener('click', () => {
        categoryFilters.forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
        applyFilters();
      });
    });

    // Price slider
    priceSlider.addEventListener('input', () => {
      priceRangeDisplay.textContent = `$0 – $${priceSlider.value}`;
      applyFilters();
    });

    // Sale toggle
    saleToggle.addEventListener('change', applyFilters);

    // Search
    searchInput.addEventListener('input', applyFilters);

    // Cart modal
    cartBtn.addEventListener('click', () => {
      cartModal.classList.add('open');
      document.body.style.overflow = 'hidden';
    });
    closeCart.addEventListener('click', () => {
      cartModal.classList.remove('open');
      document.body.style.overflow = '';
    });
    cartModal.addEventListener('click', (e) => {
      if (e.target === cartModal) {
        cartModal.classList.remove('open');
        document.body.style.overflow = '';
      }
    });

    // Newsletter
    newsletterForm.addEventListener('submit', (e) => {
      e.preventDefault();
      if (newsletterEmail.checkValidity()) {
        alert('Thank you for subscribing!');
        newsletterEmail.value = '';
      }
    });

    // Checkout button
    checkoutBtn.addEventListener('click', () => {
      alert('Checkout is not implemented in this demo. In a real app, this would redirect to a secure payment page.');
    });

    // ===== INITIALIZE =====
    document.addEventListener('DOMContentLoaded', () => {
      initSlider();
      renderProducts();
      updateCartUI();
    });
  </script>
</body>
</html>

